import React from "react";

const IconSelection = ({id}) => {
  return (
   {
    switch (id === "") {
      case value:
          
        break;
    
      default:
        break;
    }
   }
  );
};

export default IconSelection;
